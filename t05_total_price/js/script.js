const total = (addCount, addPrice, currentTotal) => {
    currentTotal += addCount * addPrice
    return currentTotal
}

let sum = 0

    alert("Insert into console for testing: \nconsole.log(sum = total(prompt('count'), prompt('price'), sum))")
